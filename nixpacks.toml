[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npm install -g @brightdata/mcp"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
